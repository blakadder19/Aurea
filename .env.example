# Supabase — proyecto "aurea" (datos reales, Fase 1: banca conectada).
# Copia este archivo a .env.local y rellena con los valores reales.
# Ambas son claves PÚBLICAS (protegidas por RLS), seguras de exponer en el
# navegador. Nunca uses aquí una clave "service-role" ni "secret".

VITE_SUPABASE_URL=https://TU-PROYECTO.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_tu-clave-publica
