/** Se ve un instante mientras llega el código de una pantalla — nunca un blanco vacío. */
export function RouteFallback() {
  return <div className="flex flex-1 items-center justify-center p-8 text-base text-ink-muted">Cargando…</div>
}
